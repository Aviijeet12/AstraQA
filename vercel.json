{
  "buildCommand": "corepack prepare pnpm@8.6.0 --activate && pnpm install && pnpm build",
  "installCommand": "corepack prepare pnpm@8.6.0 --activate && pnpm install",
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  }
}